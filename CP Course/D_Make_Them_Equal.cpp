#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    while(t--){
        int n, k; cin>>n>>k;

        vector<int> c(n), b(n);
        for(int i=0;i<n;++i) cin>>b[i];
        for(int i=0;i<n;++i) cin>>c[i];

        vector<vector<int>> dp(n+1, vector<int>(k+1, 0));

        for(int i=1;i<=n;i++){
            for(int j=1;j<=k;++j){

            }
        }

    }
    return 0;
}