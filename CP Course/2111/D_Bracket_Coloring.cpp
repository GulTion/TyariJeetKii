#include<bits/stdc++.h>

using namespace std;

int main(){
    int T; cin>>T;
    while(T--){
        int N; cin>>N;
        string s; cin>>s;

        stack<int> s;
        int color=1;
        for(char i:s){
            if(i==')') s.push_back();
        }
    }

    return 0;
}